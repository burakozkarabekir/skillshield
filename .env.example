# =============================================================================
# SkillShield Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local — it's in .gitignore.
#
# In production, set these in Vercel Dashboard → Settings → Environment Variables.
# =============================================================================

# --- App ---
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --- Vercel KV (auto-injected when you link a KV store in Vercel dashboard) ---
# For local dev: get these from Vercel Dashboard → Storage → KV → Settings
KV_REST_API_URL=
KV_REST_API_TOKEN=

# --- Stripe ---
# Get from https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
# Get from Stripe CLI: stripe listen --forward-to localhost:3000/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_...

# --- Optional: Upstash Redis (for rate limiting if you want separate from KV) ---
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# --- Optional: Resend (transactional email) ---
# RESEND_API_KEY=re_...

# --- Optional: Plausible Analytics ---
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=skillshield.dev
